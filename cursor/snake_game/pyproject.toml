[project]
name = "snake-game"
version = "0.1.0"
description = "Classic Snake game built with Python and Pygame using uv for dependency management"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame>=2.5.0",
]

[tool.uv]
dev-dependencies = []
